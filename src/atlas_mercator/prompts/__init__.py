"""System prompt templates.

Each agent has its own module so prompts can be reviewed and versioned
independently of the agent runtime. See ``prompts/listing_optimizer.py``
for the canonical example.
"""
